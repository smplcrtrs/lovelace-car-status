node_modules/
*.tsbuildinfo
.DS_Store

# dist/car-status.js is committed on purpose: with no GitHub release to scan,
# HACS validates the default branch and requires the built .js to be present
# under dist/ or the repo root. Source maps stay out.
dist/*.map
